# Model catalog

Paseo's provider catalog controls selectable/default models in Paseo. The room runtime independently receives a Codex model catalog generated by `codex debug models`.

Keep the Paseo provider model ID, overlay `model`, and expected Codex catalog aligned. The sync script fails if Codex returns no models.

