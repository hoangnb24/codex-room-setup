# MCP injection

Paseo MCP injection is enabled only for:

```text
codex-supervisor
codex-lead
```

Peer and Review are excluded. Review also removes MCP server tables inherited from the operator's base Codex configuration.

